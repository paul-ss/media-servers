version: "3.9"
services:
  app:
    build: .
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    volumes:
      - ../salt:/home/salt
      - ../configs:/home/configs
    command: /bin/bash
